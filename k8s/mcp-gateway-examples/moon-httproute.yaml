apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: moon-route
  namespace: mcp-test
  labels:
    mcp-server: "true"
spec:
  parentRefs:
    - name: mcp-gateway
      namespace: mcp-system
  hostnames:
    - moon.mcp.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: moon-service
          namespace: moon-server
          port: 8081
