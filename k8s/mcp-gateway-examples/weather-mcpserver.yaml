apiVersion: mcp.kagenti.com/v1alpha1
kind: MCPServer
metadata:
  name: weather-mcp-server
  namespace: mcp-test
  labels:
    kagenti/mcp: "true"
spec:
  toolPrefix: weather_
  path: /mcp
  targetRef:
    group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: weather-route
