apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: quotes-route
  namespace: mcp-test
  labels:
    mcp-server: "true"
spec:
  parentRefs:
    - name: mcp-gateway
      namespace: mcp-system
  hostnames:
    - quotes.mcp.local
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: quotes-service
          namespace: quotes-server
          port: 8082
