apiVersion: apps/v1
kind: Deployment
metadata:
  name: moon-server
  namespace: moon-server
  labels:
    app: moon-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: moon-server
  template:
    metadata:
      labels:
        app: moon-server
    spec:
      # Allow scheduling on tainted nodes (single-node Kind cluster)
      tolerations:
      - key: node-role.kubernetes.io/control-plane
        operator: Exists
        effect: NoSchedule
      - key: node-role.kubernetes.io/master
        operator: Exists
        effect: NoSchedule
      - key: node.kubernetes.io/unreachable
        operator: Exists
        effect: NoSchedule
      containers:
      - name: moon-server
        image: ghcr.io/pavelanni/moon-server:main
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8081
          name: http
        env:
        - name: MOON_SERVER_PORT
          value: "8081"
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false

